import React from 'react';
import CartForm from '../../components/ecommerce/Cart/CartForm';

const CartScreen = () => {
    return (
        <section className="cart_container">
            <CartForm />
        </section>
    );
};

export default CartScreen;